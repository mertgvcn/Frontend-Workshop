//? SET - Data Structure (Aynı değerden bir tane bulunabilir.)

/*
    set.add(value) //her türden olabilir.
    set.size;
    set.delete(value)
    set.has(value)

*/

const set = new Set();

//önceki dersteki gibi for-of, foreach ile değerleri gezebiliriz
//arrayden sete, setten arraye geçebiliriz. 

